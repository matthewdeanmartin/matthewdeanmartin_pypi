[project]
name = "pypi-template-demo"
version = "0.1.0"
description = "A runnable demo app for the extracted PyPI Jinja2 design system."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1.0",
  "httpx>=0.28,<1.0",
  "jinja2>=3.1,<4.0",
  "uvicorn[standard]>=0.34,<1.0",
]

[tool.uv]
package = false
