---
ci:
  autoupdate_schedule: quarterly
  autofix_commit_msg: |
    [pre-commit.ci] Apply automatic pre-commit fixes

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
      - id: check-builtin-literals
      - id: fix-byte-order-marker
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: check-symlinks
      - id: check-toml
      - id: debug-statements
      - id: detect-private-key
      - id: forbid-new-submodules

  # Mirrors `make format` exactly: isort -> black -> ruff --fix -> black.
  # Explicit config paths required: pre-commit runs from repo root and this
  # is a monorepo sub-package — without them tools fall back to defaults or
  # the wrong package config.
  - repo: local
    hooks:
      - id: fix
        name: fix (isort + black + ruff + black)
        language: system
        entry: bash -c 'uv run isort --settings-path john_doe/ "$@" && uv run black --config john_doe/pyproject.toml "$@" && uv run ruff check --fix --quiet --config john_doe/pyproject.toml "$@" && uv run black --config john_doe/pyproject.toml "$@"' --
        types: [python]
        files: ^john_doe/
        pass_filenames: true

  - repo: https://github.com/pre-commit/pygrep-hooks
    rev: v1.10.0
    hooks:
      - id: python-use-type-annotations
        files: ^john_doe/
      - id: python-no-log-warn
        files: ^john_doe/
      - id: text-unicode-replacement-char
        files: ^john_doe/
