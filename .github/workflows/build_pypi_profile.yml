---
name: Build pypi_profile

on:
  push:
    paths:
      - ".github/workflows/build_pypi_profile.yml"
      - ".github/workflows/publish_pypi_profile.yml"
      - "pypi_profile/**"
      - "pypi_ds/**"
      - "john_doe/**"
      - "pyproject.toml"
      - "uv.lock"
  pull_request:
    paths:
      - ".github/workflows/build_pypi_profile.yml"
      - ".github/workflows/publish_pypi_profile.yml"
      - "pypi_profile/**"
      - "pypi_ds/**"
      - "john_doe/**"
      - "pyproject.toml"
      - "uv.lock"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: pypi_profile
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@v7
      - name: Install dependencies
        run: uv sync
      - name: Run quality gates
        run: uv run make check-ci
      - name: Build distribution
        run: uv build
      - name: Upload distribution artifacts
        uses: actions/upload-artifact@v7
        with:
          name: pypi_profile-dist
          path: pypi_profile/dist/
          if-no-files-found: error
          retention-days: 7
